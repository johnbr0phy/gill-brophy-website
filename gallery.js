const $=id=>document.getElementById(id);let works=[],visible=[],current=null,opener=null;
const params=new URLSearchParams(location.search);$('search').value=params.get('q')||'';
async function load(){try{$('gallery-error').hidden=true;const r=await fetch('/gill-brophy-website/works.json');if(!r.ok)throw Error();({works}=await r.json());const names=[...new Set(works.map(w=>w.collection).filter(Boolean))].sort();$('collection').replaceChildren(new Option('All collections',''));names.forEach(n=>$('collection').add(new Option(n,n)));$('collection').value=params.get('collection')||'';render();fromHash();}catch(e){$('gallery-error').hidden=false;$('work-count').textContent='Collection unavailable';}}
function render(){const q=$('search').value.trim().toLowerCase(),collection=$('collection').value;visible=works.filter(w=>(!collection||w.collection===collection)&&(!q||(w.title+' '+w.collection+' '+w.description).toLowerCase().includes(q)));if($('sort').value==='title')visible.sort((a,b)=>a.title.localeCompare(b.title));if($('sort').value==='newest')visible.sort((a,b)=>b.created-a.created);$('gallery').replaceChildren();$('empty').hidden=visible.length!==0;$('work-count').textContent=visible.length+' '+(visible.length===1?'painting':'paintings');for(const w of visible){const b=document.createElement('button');b.className='work-card';b.setAttribute('aria-label','View '+w.title);const img=document.createElement('img');img.src=w.thumbnail;img.alt=w.title;img.width=w.width;img.height=w.height;img.loading='lazy';img.decoding='async';const caption=document.createElement('div');caption.className='work-caption';const h=document.createElement('h2');h.textContent=w.title;const c=document.createElement('small');c.textContent=w.collection;caption.append(h,c);b.append(img,caption);b.addEventListener('click',()=>{opener=b;open(w.id)});$('gallery').append(b)}const p=new URLSearchParams();if(q)p.set('q',$('search').value);if(collection)p.set('collection',collection);history.replaceState(null,'','/gill-brophy-website/gallery.html'+(p.size?'?'+p.toString():'')+location.hash);}
function open(id){const w=works.find(w=>w.id===id);if(!w)return;current=w;$('viewer-title').textContent=w.title;$('viewer-details').textContent=[w.medium,w.year,w.dimensions].filter(Boolean).join(' · ');$('viewer-description').textContent=w.description;$('viewer-image').alt=w.title;$('image-error').hidden=true;$('viewer-image').src=w.image;const list=visible.some(x=>x.id===id)?visible:works;const index=list.findIndex(x=>x.id===id);$('previous').disabled=index<=0;$('next').disabled=index>=list.length-1;if(!$('viewer').open){$('viewer').showModal();document.body.style.overflow='hidden';}history.replaceState(null,'',location.pathname+location.search+'#'+id);}
function step(delta){const list=visible.some(x=>x.id===current?.id)?visible:works;const i=list.findIndex(w=>w.id===current?.id),w=list[i+delta];if(w)open(w.id)}
function fromHash(){if(location.hash)open(location.hash.slice(1));}
$('viewer-close').addEventListener('click',()=>$('viewer').close());$('viewer').addEventListener('close',()=>{document.body.style.overflow='';history.replaceState(null,'',location.pathname+location.search);if(opener?.isConnected)opener.focus();});$('viewer-image').addEventListener('error',()=>$('image-error').hidden=false);$('previous').addEventListener('click',()=>step(-1));$('next').addEventListener('click',()=>step(1));$('viewer').addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();step(-1)}if(e.key==='ArrowRight'){e.preventDefault();step(1)}});['search','collection','sort'].forEach(id=>$(id).addEventListener(id==='search'?'input':'change',render));$('clear').addEventListener('click',()=>{$('search').value='';$('collection').value='';render()});$('retry').addEventListener('click',load);addEventListener('hashchange',fromHash);load();
